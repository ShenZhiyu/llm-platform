fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg[binary]>=3.2.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
httpx>=0.27.0
pytest>=8.0.0
pypdf>=5.0.0
python-docx>=1.1.0
# 以下重型 RAG 包默认不安装以避免拉取 CUDA/torch（约 2.5GB）。
# chromadb 为延迟 import（rag_service.py 中 try/except 兜底），缺失时 RAG 索引相关功能不可用但不影响启动。
# 若需启用本地向量库，请手动：uv pip install chromadb sentence-transformers
# llama-index>=0.12.0
# llama-index-vector-stores-chroma>=0.4.0
# llama-index-embeddings-huggingface>=0.5.0
# chromadb>=0.5.0
# sentence-transformers>=3.0.0
